services:
  db:
    image: postgres:15
    container_name: blog-db
    environment:
      POSTGRES_USER: MOUAD
      POSTGRES_PASSWORD: mmm
      POSTGRES_DB: blogdb
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data: